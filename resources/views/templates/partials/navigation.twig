<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}">Laravel like with Slim</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
           <ul class="nav navbar-nav navbar-right">
               {% if auth.check %}
                   <li class="dropdown">
                       <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                          aria-expanded="true">
                           {{ auth.user.name | capitalize}} <span class="caret"></span>
                       </a>
                       <ul class="dropdown-menu">
                           <li><a href="{{ path_for('auth.password.change') }}">Changer votre Mot de passe</a></li>
                           <li><a href="{{ path_for('auth.logout') }}">Deconnexion</a></li>
                       </ul>
                   </li>
               {% else %}
                   <li><a href="{{ path_for('auth.signin') }}">Connexion</a></li>
                   <li><a href="{{ path_for('auth.signup') }}">Inscription</a></li>
               {% endif %}
               <li>
                   <a href="{{ path_for('cart.index') }}"><span class="glyphicon glyphicon-shopping-cart" aria-hidden="true">Cart({{ basket.count }})</span></a>
               </li>
           </ul>
        </div><!--/.navbar-collapse -->
    </div>
</nav>